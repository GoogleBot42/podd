# Read by the dnsmasq instance NetworkManager spawns for shared-mode (AP)
# connections — i.e. only while the podd-setup provisioning AP is active.
# Resolve every name to the pod so clients' captive-portal detection opens
# the setup form instead of timing out.
address=/#/10.42.0.1
