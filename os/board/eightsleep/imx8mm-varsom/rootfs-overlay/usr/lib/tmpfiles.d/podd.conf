# Create podd's state dir on the persistent partition early (after /data mounts,
# before services) so podd.service's WorkingDirectory=/data/podd exists on the
# very first boot — otherwise systemd fails the unit with status=200/CHDIR.
d /data/podd 0755 root root -
