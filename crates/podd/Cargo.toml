[package]
name = "podd"
description = "Open control daemon for the Eight Sleep Pod (fork of opensleep)"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[[bin]]
name = "podd"
path = "src/main.rs"

[dependencies]
podd-core = { path = "../podd-core" }

anyhow.workspace = true
env_logger.workspace = true
log.workspace = true
tokio.workspace = true

# NOTE: the api/schedule/update layers (axum web/API, thermostat scheduler,
# on-device update agent using `pod-update`) land here as the fork grows.
